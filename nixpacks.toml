[phases.setup]
nixPkgs = ["python3", "nodejs_20", "npm", "gcc", "libffi", "openssl", "zlib"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "python HOST.py"